import styled, { css } from 'styled-components'

export const LogoImg = styled.div`
    ${({ theme }) => css`
        width: 80px;
        height: 80px;
        img {
            width: 100%;
            height: 100%;
        }
    `}
`
